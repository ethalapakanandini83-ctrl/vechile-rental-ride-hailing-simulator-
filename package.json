{
  "name": "vehicle-rental-ride-hailing-simulator-root",
  "version": "1.0.0",
  "description": "Root orchestrator for Vehicle Rental & Ride Hailing Simulator",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
